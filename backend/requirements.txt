# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
gunicorn==21.2.0
websockets==12.0

# Configuration & Environment
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Async Support
aiofiles==23.2.1

# Cross-platform file locking (file_store.py concurrency safety)
portalocker==2.10.1

# ML - CLIP Embeddings
# open-clip-torch pulls in the default (CPU) torch/torchvision wheels. If
# this machine has an NVIDIA GPU, set CLIP_DEVICE=cuda in .env AND reinstall
# torch/torchvision matching your driver's CUDA version afterwards, e.g.:
#   pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 \
#     --index-url https://download.pytorch.org/whl/cu128
# (pick the cuXXX index that matches your driver — check with `nvidia-smi`;
# torch and torchvision versions must come from the same index/build).
open-clip-torch==2.24.0
Pillow==10.2.0
numpy>=1.24.0

# ML - DINOv2 backbone for the primary embedding model (app/services/
# embedding.py's Dinov2HeadEmbeddingService), and used by the retrieval
# evaluation harness / training script to benchmark backbones. Already
# pulled in transitively by open-clip-torch; pinned explicitly here since
# it's now imported directly by production code.
timm==1.0.28

# Rate Limiting
slowapi==0.1.9

# Observability - Prometheus & OpenTelemetry
prometheus-client==0.19.0
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0

# Testing
pytest==7.4.4
pytest-asyncio==0.21.1
httpx==0.26.0

# Logging
python-json-logger==2.0.7

# Code Quality (linting/formatting - optional but recommended)
black==23.12.1
ruff==0.1.11
