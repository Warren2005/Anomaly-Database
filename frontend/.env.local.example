# Copy this file to .env.local (gitignored) only if the backend runs on a
# port other than 8000 on your machine. vite.config.js reads BACKEND_PORT
# to set the dev-server's /api proxy target — see RUNNING_INSTRUCTIONS.md.
BACKEND_PORT=8000
